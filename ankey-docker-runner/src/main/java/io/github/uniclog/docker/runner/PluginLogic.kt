package io.github.uniclog.docker.runner

import com.intellij.openapi.actionSystem.AnActionEvent

object PluginLogic {
    fun run(event: AnActionEvent, data: Map<String, String>) {


        // get port info
        // val placeholders = Placeholders()
        // placeholders.num = "demo"
        // placeholders.portHttp = "8082"

        // generate docker compose
        // val template = loadComposeTemplate(composePath)
        // val compose = replacePlaceholders(template, placeholders)
        // println(compose)

        // run docker
        // DockerComposeRunner.runCompose()
        println("Docker Run 3..")
    }


}